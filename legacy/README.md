# Legacy Files

The original flat-layout files remain in place while the new
`marsdog_control` package is introduced. This directory records the intended
legacy group without moving files yet.

Legacy candidates:

- `mocap_to_real/walk2.py`
- `mocap_to_real/motor_lz.py`
- `mocap_to_real/*.bak_pre_merge`

Keep these files available until the new package entry points have fully
replaced the old workflow on the robot.
